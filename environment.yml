name: jain-digitizer
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - rich
  - pyinstaller
  - pip:
      - google-genai
      - streamlit
      - streamlit-quill
      - pyside6
